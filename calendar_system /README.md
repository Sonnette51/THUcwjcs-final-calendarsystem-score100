# 用到的库
标准库：
sys os datetime json time tkinter
第三方库：
lunarcalendar

# 可能需要干的事
- 如果没有：
创建虚拟环境）python3 -m venv venv
激活source venv/bin/activate
安装依赖pip install -r requirements.txt
- 必须要干：
control+shift+p选择解释器
运行main.py

# 其他声明
1. 是在vscode里写的
1. 我是用Macbook测试下来的，不知道用win会不会冒出什么奇妙的bug
2. UI部分用过ai，不然得写到下辈子了
