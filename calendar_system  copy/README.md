# 用到的库
标准库：
sys os datetime json time tkinter
第三方库：
lunarcalendar

# 可能需要干的事
创建虚拟环境（如果没有）python3 -m venv venv
激活source venv/bin/activate
安装依赖（如果没有）pip install -r requirements.txt

运行main.py

# 声明
1. 我是用Macbook测试下来的，不知道用win会不会冒出什么奇妙的bug
2. UI部分用过ai，不然得写到下辈子了

# 一个小问题
在日期查询页面修改（删除）完后再去事件管理页面是修改不了的